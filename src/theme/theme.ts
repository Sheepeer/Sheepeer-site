import { createTheme } from "@mui/material"

const theme = createTheme({
  palette: {
    primary: {
      main: '#2e2e2c'
    },
    secondary: {
      main: '#e99469'
    }
  }
})

export default theme