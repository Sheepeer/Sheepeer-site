import { createTheme } from "@mui/material"

const theme = createTheme({
  palette: {
    primary: {
      main: '#2e2e2c'
    },
    secondary: {
      main: '#c77500'
    }
  }
})

export default theme